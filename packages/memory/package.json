{
  "name": "@lira/memory",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "db:migrate": "echo 'Run migrations via docker compose exec postgres psql'",
    "db:seed": "echo 'Seed via scripts/seed-memories.ts'"
  },
  "dependencies": {
    "@lira/shared-types": "*",
    "@qdrant/js-client-rest": "^1.12.0",
    "@supabase/supabase-js": "^2.105.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
